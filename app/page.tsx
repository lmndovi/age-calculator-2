import Body from "@/components/Body";
import Header from "@/components/Header";
import Image from "next/image";

export default function Home() {
  return (
    <main>
      <section>
        <Header />
        {/* MainNav */}
        {/* SecondaryNav */}
      </section>
      <section>
        <Body />
        {/* Boxes on Left */}
        {/* Main section,  team selector, Win rate, Additional Info */}
        {/*Boxes on the right  */}
      </section>
    </main>
  );
}
